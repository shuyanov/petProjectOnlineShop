export interface TextLinkType {
  href: string
  text: string
}

export interface ButtonIconProps {
  onClick?: () => void
  src: string
}

export interface ButtonProps {
  onClick?: () => void
  title: string
}
